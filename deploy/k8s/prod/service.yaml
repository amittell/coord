apiVersion: v1
kind: Service
metadata:
  name: coord
  namespace: coord
  labels:
    app.kubernetes.io/name: coord
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: coord
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
