services:
  coordination:
    build:
      context: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - coordination-data:/data
    restart: unless-stopped

volumes:
  coordination-data:
