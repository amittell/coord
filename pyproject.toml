[project]
name = "coord-mcp-server"
version = "0.43.0"
description = "Coordination service for multi-engineer agent teams (HTTP API + MCP + dashboard)"
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [{name = "Alex Mittell"}]
keywords = [
    "agents",
    "coordination",
    "mcp",
    "claude-code",
    "codex",
    "cursor",
    "multi-agent",
    "file-locking",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Software Development :: Version Control",
    "Typing :: Typed",
]
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.45.0",
    "pydantic>=2.13.3",
    "pydantic-settings>=2.14.0",
    "mcp>=1.2.0",
    "httpx>=0.28.0",
    "pathspec>=0.12.1",
    "aiosqlite>=0.20.0",
    "pyyaml>=6.0.2",
    "packaging>=23.0",
    "PyJWT[crypto]>=2.8",
]

[project.optional-dependencies]
symbols = [
    "tree-sitter>=0.25.2",
    "tree-sitter-typescript>=0.23.2",
    "tree-sitter-python>=0.23.0",
    "tree-sitter-go>=0.23.0",
    "tree-sitter-javascript>=0.23.0",
    "tree-sitter-rust>=0.23.0",
    "tree-sitter-java>=0.23.0",
    "tree-sitter-c>=0.23.0",
    "tree-sitter-cpp>=0.23.0",
    "tree-sitter-c-sharp>=0.23.0",
    "tree-sitter-ruby>=0.23.0",
    "tree-sitter-php>=0.23.0",
    "tree-sitter-kotlin>=0.23.0",
    "tree-sitter-swift>=0.7.2",
    "tree-sitter-scala>=0.23.0",
]
otel = [
    "opentelemetry-sdk>=1.30.0",
    "opentelemetry-exporter-otlp-proto-http>=1.30.0",
    "opentelemetry-instrumentation-fastapi>=0.51b0",
    "opentelemetry-instrumentation-httpx>=0.51b0",
]
dev = [
    "pytest>=9.0.3",
    "pytest-asyncio>=1.4.0",
    "ruff>=0.15.15",
    "mypy>=2.1.0",
    "tree-sitter>=0.25.2",
    "tree-sitter-typescript>=0.23.2",
    "tree-sitter-python>=0.23.0",
    "tree-sitter-go>=0.23.0",
    "tree-sitter-javascript>=0.23.0",
    "tree-sitter-rust>=0.23.0",
    "tree-sitter-java>=0.23.0",
    "tree-sitter-c>=0.23.0",
    "tree-sitter-cpp>=0.23.0",
    "tree-sitter-c-sharp>=0.23.0",
    "tree-sitter-ruby>=0.23.0",
    "tree-sitter-php>=0.23.0",
    "tree-sitter-kotlin>=0.23.0",
    "tree-sitter-swift>=0.7.2",
    "tree-sitter-scala>=0.23.0",
    "opentelemetry-sdk>=1.30.0",
    "opentelemetry-exporter-otlp-proto-http>=1.30.0",
    "opentelemetry-instrumentation-fastapi>=0.51b0",
    "opentelemetry-instrumentation-httpx>=0.51b0",
]

[project.scripts]
coord = "coordination.cli:main"
coord-mcp = "coordination.mcp_server:main"
coord-api = "coordination.main:run"

[project.urls]
Homepage = "https://github.com/amittell/coord"
Repository = "https://github.com/amittell/coord"
Changelog = "https://github.com/amittell/coord/blob/main/CHANGELOG.md"
Issues = "https://github.com/amittell/coord/issues"
Documentation = "https://github.com/amittell/coord/tree/main/docs"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["coordination"]

[tool.hatch.build.targets.sdist]
include = [
    "coordination",
    "README.md",
    "pyproject.toml",
    "docs",
    "templates",
    "scripts",
    ".env.example",
    "compose.yaml",
    "Dockerfile",
    ".github",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.mypy]
python_version = "3.11"
strict = false
ignore_missing_imports = true
warn_unused_ignores = true
warn_redundant_casts = true
check_untyped_defs = true
disallow_incomplete_defs = false
no_implicit_optional = true
disable_error_code = ["import-untyped"]
exclude = [
    "^\\.venv/",
    "^build/",
    "^dist/",
    "^tests/",
]

[[tool.mypy.overrides]]
module = "mcp.*"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "yaml"
ignore_missing_imports = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
    "integration: end-to-end tests that spawn real processes (deselect with -m 'not integration')",
]
